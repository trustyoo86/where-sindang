# Repository Instructions

## Design System

This project has a design reference at `.claude/DESIGN.md`.

When making UI, layout, styling, or frontend component changes, read and follow `.claude/DESIGN.md` before editing. Treat it as the source of truth for visual direction, tokens, component styling, imagery, spacing, and interaction details.

Keep `.claude/DESIGN.md` as the canonical design document. Do not duplicate its full contents here; update the source document when design guidance changes.
